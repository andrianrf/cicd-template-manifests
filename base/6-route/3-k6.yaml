apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: k6
spec:
  to:
    name: k6
    weight: 100
    kind: Service
  tls:
    insecureEdgeTerminationPolicy: Allow
    termination: edge
  port:
    targetPort: 5665