apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: pgadmin
spec:
  to:
    name: pgadmin
    weight: 100
    kind: Service
  tls:
    insecureEdgeTerminationPolicy: Allow
    termination: edge
  port:
    targetPort: 8080