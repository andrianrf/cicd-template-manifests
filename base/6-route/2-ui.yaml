apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ui
spec:
  to:
    name: ui
    weight: 100
    kind: Service
  tls:
    insecureEdgeTerminationPolicy: Allow
    termination: edge
  port:
    targetPort: 8080